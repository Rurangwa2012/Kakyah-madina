# Copy to .env.local. Never commit real keys.
# Supabase → Project Settings → API

# Vercel: add these as type "Config" (not Sensitive).
# NEXT_PUBLIC_ is required so the cashier browser can talk to Supabase.
# The publishable/anon key is safe in the browser; Row Level Security protects data.
NEXT_PUBLIC_SUPABASE_URL=https://dspwrfcjdcowuymjazze.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Vercel: type "Sensitive". Never name this NEXT_PUBLIC_*
SUPABASE_SERVICE_ROLE_KEY=
